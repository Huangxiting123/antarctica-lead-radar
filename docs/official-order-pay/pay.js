const params=new URLSearchParams(location.search);const get=(key,fallback)=>(params.get(key)||fallback).slice(0,120);const brand=get("brand","好友代付"),merchant=get("merchant","待核验收款主体"),service=get("service","待核验订单内容"),refund=get("refund","请联系商户确认退款规则"),order=get("order","DEMO-ORDER");const amount=Math.min(520,Math.max(9.9,Number(params.get("amount"))||9.9));const expires=Number(params.get("expires"))||Date.now()+15*60*1000;const money=n=>new Intl.NumberFormat("zh-CN",{style:"currency",currency:"CNY"}).format(n);document.title=brand+"｜好友代付 "+money(amount);document.querySelector("#p-brand-head").textContent=brand;document.querySelector("#p-brand").textContent=brand;document.querySelector("#p-merchant").textContent=merchant;document.querySelector("#p-service").textContent=service;document.querySelector("#p-refund").textContent=refund;document.querySelector("#p-order").textContent=order;document.querySelector("#p-amount").textContent=money(amount);const time=document.querySelector("#p-time");function tick(){const left=Math.max(0,Math.ceil((expires-Date.now())/1000));time.textContent=left?String(Math.floor(left/60)).padStart(2,"0")+":"+String(left%60).padStart(2,"0"):"已过期";time.classList.toggle("expired",!left)}tick();setInterval(tick,1000);const dl=document.querySelector("#p-dl"),details=document.querySelector("#p-details");details.addEventListener("click",()=>{const open=dl.classList.toggle("open");details.textContent=open?"收起详情":"查看详情"});const message=document.querySelector("#p-message");document.querySelectorAll("[data-channel]").forEach(button=>button.addEventListener("click",()=>{message.hidden=false;message.textContent=button.dataset.channel+"尚未开通。当前页面仅用于确认代付样式，不会发起扣款。商户完成认证后才会跳转官方收银台。"}));document.querySelector("#p-share").addEventListener("click",async()=>{try{if(navigator.share){await navigator.share({title:brand+"｜好友代付",text:service+"，金额 "+money(amount)+"，请帮我完成代付。",url:location.href});return}await navigator.clipboard.writeText(location.href);message.hidden=false;message.textContent="链接已复制，可粘贴发送到微信或 QQ。"}catch{message.hidden=false;message.textContent="未完成分享，请复制浏览器地址后发送给好友。"}});
